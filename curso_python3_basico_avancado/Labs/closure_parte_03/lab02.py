"""
Funções nomeadas em Python
"""

def div(x, y):
    """Função que soma dois valores"""
    return x / y


print(list(map(lambda x: x + 2, [1, 2, 3])))

# Função anônima
# soma = lambda x: x + 2
# print(soma(2))
