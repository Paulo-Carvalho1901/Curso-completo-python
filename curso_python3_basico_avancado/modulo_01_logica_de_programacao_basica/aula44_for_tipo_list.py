"""
for in com listas

como as lista é um iterável utilizaremos o for
para iterar sobre cada elemento dessa lista
"""

lista = ['Maria', 'Helena', 'Luiz']

for nome in lista:
    print(nome, type(nome))
